var INPUT = process.argv;
var opt = INPUT['2'];
var stat = INPUT['3'];

function solver(options, staticrepeat) {
  var answer = ((1 / options)^staticrepeat);
  console.log(answer);
  console.log("xd ");
}
solver(opt, stat);
